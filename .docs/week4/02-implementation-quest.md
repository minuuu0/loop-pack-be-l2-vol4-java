# Week 4 — Implementation Quest

> 주문 시, **재고 / 주문 / 쿠폰의 정합성을 트랜잭션으로 보장**하고 동시성 이슈를 제어한다.

## Must-Have (무조건 가져가야 할 것)

- DB 트랜잭션
- Lock
- 동시성 테스트
- 쿠폰 개념

---

## 🎟 쿠폰 (Coupons)

- 주문 시 사용자가 소유한 쿠폰을 적용해 할인받을 수 있다.
- 쿠폰은 **정액(FIXED) / 정률(RATE)** 두 종류가 있으며 **재사용 불가**.
- 존재하지 않거나 사용 불가능한 쿠폰으로 요청 시 **주문은 실패**해야 한다.

### 대고객 API

| METHOD | URI | user_required | 설명 |
| --- | --- | --- | --- |
| POST | `/api/v1/coupons/{couponId}/issue` | O | 쿠폰 발급 요청 |
| GET | `/api/v1/users/me/coupons` | O | 내 쿠폰 목록 조회 |

> 쿠폰 목록 조회 시 `AVAILABLE` / `USED` / `EXPIRED` 상태를 함께 반환한다.

### 🏷 쿠폰 ADMIN

| METHOD | URI | ldap_required | 설명 |
| --- | --- | --- | --- |
| GET | `/api-admin/v1/coupons?page=0&size=20` | O | 쿠폰 템플릿 목록 조회 |
| GET | `/api-admin/v1/coupons/{couponId}` | O | 쿠폰 템플릿 상세 조회 |
| POST | `/api-admin/v1/coupons` | O | 쿠폰 템플릿 등록 (FIXED / RATE 지정) |
| PUT | `/api-admin/v1/coupons/{couponId}` | O | 쿠폰 템플릿 수정 |
| DELETE | `/api-admin/v1/coupons/{couponId}` | O | 쿠폰 템플릿 삭제 |
| GET | `/api-admin/v1/coupons/{couponId}/issues?page=0&size=20` | O | 특정 쿠폰의 발급 내역 조회 |

**쿠폰 템플릿 등록 요청 예시**

```json
{
  "name": "신규가입 10% 할인",
  "type": "RATE",            // FIXED | RATE
  "value": 10,               // 정률: 퍼센트(%), 정액: 할인 금액(원)
  "minOrderAmount": 10000,   // 최소 주문 금액 조건 (선택)
  "expiredAt": "2026-12-31T23:59:59"
}
```

---

## 🧾 주문 API 변경사항

**요청 예시 (쿠폰 적용)**

```json
{
  "items": [
    { "productId": 1, "quantity": 2 },
    { "productId": 3, "quantity": 1 }
  ],
  "couponId": 42   // 미적용 시 생략 가능 (NULLABLE)
}
```

**쿠폰 적용 규칙**

- 쿠폰은 주문 1건당 **1장만** 적용 가능.
- 존재하지 않거나 이미 사용된 쿠폰, 만료된 쿠폰, **타 유저 소유 쿠폰**으로 요청 시 주문 실패.
- 주문 성공 시 해당 쿠폰은 즉시 `USED`로 변경되며 재사용 불가.
- 주문 정보 스냅샷에 **쿠폰 적용 전 금액 / 할인 금액 / 최종 결제 금액**이 모두 포함되어야 한다.

---

## 📋 과제 정보

- 주문 API에 트랜잭션을 적용하고 재고 / 쿠폰 / 주문 도메인의 정합성을 보장한다.
- 동시성 이슈(Lost Update)가 발생하지 않도록 **낙관적 락 또는 비관적 락**을 적용한다.
- 주요 구현 대상은 **Application Layer (OrderFacade 등)**에서의 트랜잭션 처리.
- 동시성 이슈가 발생할 수 있는 기능 테스트가 모두 성공해야 한다.

**주문 처리 흐름 예시**

```
1. 주문 요청
2. "주문을 위한 처리" (순서 무관)
   - 쿠폰 유효성 검증 및 사용 처리   // 동시성 이슈 위험 구간
   - 상품 재고 확인 및 차감          // 동시성 이슈 위험 구간
3. 주문 엔티티 생성 및 저장
```

### 🚀 구현 보강

- 모든 API가 요구사항 기반으로 동작해야 한다.
- 미비한 구현을 모두 완성한다.

---

## ✅ Checklist

### 🗞️ Coupon 도메인

- [ ] 쿠폰은 사용자가 소유하며, 이미 사용된 쿠폰은 사용할 수 없다.
- [ ] 쿠폰 종류는 정액 / 정률로 구분되며 각 적용 로직을 구현했다.
- [ ] 각 발급된 쿠폰은 최대 한 번만 사용될 수 있다.

### 🧾 주문

- [ ] 주문 전체 흐름에 대해 원자성이 보장된다.
- [ ] 사용 불가능하거나 존재하지 않는 쿠폰일 경우 주문은 실패한다.
- [ ] 재고가 존재하지 않거나 부족할 경우 주문은 실패한다.
- [ ] 쿠폰·재고 처리 중 하나라도 실패하면 모두 롤백된다.
- [ ] 주문 성공 시 모든 처리가 정상 반영된다.

### 🧪 동시성 테스트

- [ ] 동일 상품에 여러 명이 좋아요/싫어요를 요청해도 좋아요 수가 정상 반영된다.
- [ ] 동일 쿠폰으로 여러 기기에서 동시 주문해도 쿠폰은 단 한 번만 사용된다.
- [ ] 동일 상품에 여러 주문이 동시 요청되어도 재고가 정상 차감된다.

---

## 📡 집중할 점

> 모든 기능을 개발한 후, **동시성·멱등성·일관성·느린 조회·동시 주문** 등 실제 서비스 문제를 해결한다.
>
> **낙관적 락 / 비관적 락** 중 각 도메인 특성에 맞는 전략을 선택해 적용하라.
> **Application Layer(OrderFacade)에서의 트랜잭션 경계 설계가 핵심**이다.

---

## ✍️ Technical Writing Quest

테크노트(GitHub Issues) 또는 블로그 중 편한 방법으로 작성 후 링크 제출.

**Issue 포맷 (4개 중 선택)**
1. 📐 Design Doc — 설계 의사결정 중심
2. 🪞 Retrospective — 과제 회고·트러블슈팅
3. ⚔️ Challenge Story — 도전 → 해결 압축 서사
4. 📊 Benchmark Report — A vs B 직접 측정

- Java Issue: https://github.com/loopers-labs/loop-pack-be-l2-vol4-java/issues/new/choose

**주제 추천**
- 트랜잭션이면 안전한 줄 알았다 → rollback 안 되는 경우 실험
- 쿠폰 사용 처리에는 어떤 락이 적절할까?
- 같은 상품을 10명이 동시에 주문했을 때 발생한 일
- 비관적 락 vs 낙관적 락, 선택 기준은 무엇이었나?
